---

---

<div class="max-w-2xl mx-auto">
    <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6 text-center">
        Contáctanos
    </h1>
    <p class="text-gray-600 text-center mb-8">
        ¿Tienes alguna pregunta o comentario? Envíanos un mensaje y te
        responderemos lo antes posible.
    </p>

    <form
        action="https://formsubmit.co/contacto@focoempleo.com"
        method="POST"
        class="space-y-6 bg-white p-6 md:p-8 rounded-2xl shadow-sm border border-gray-100"
    >
        <input
            type="hidden"
            name="_subject"
            value="Nuevo mensaje de Foco Empleo"
        />
        <input type="hidden" name="_captcha" value="false" />

        <div>
            <label
                for="name"
                class="block text-sm font-medium text-gray-700 mb-2"
            >
                Nombre completo
            </label>
            <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-transparent transition-colors"
                placeholder="Tu nombre"
            />
        </div>

        <div>
            <label
                for="email"
                class="block text-sm font-medium text-gray-700 mb-2"
            >
                Correo electrónico
            </label>
            <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-transparent transition-colors"
                placeholder="tu@email.com"
            />
        </div>

        <div>
            <label
                for="subject"
                class="block text-sm font-medium text-gray-700 mb-2"
            >
                Asunto
            </label>
            <input
                type="text"
                id="subject"
                name="subject"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-transparent transition-colors"
                placeholder="¿En qué podemos ayudarte?"
            />
        </div>

        <div>
            <label
                for="message"
                class="block text-sm font-medium text-gray-700 mb-2"
            >
                Mensaje
            </label>
            <textarea
                id="message"
                name="message"
                required
                rows="5"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-teal-500 focus:border-transparent transition-colors resize-none"
                placeholder="Escribe tu mensaje aquí..."></textarea>
        </div>

        <button
            type="submit"
            class="w-full bg-teal-800 hover:bg-teal-900 text-white font-semibold py-3 px-6 rounded-lg transition-colors shadow-sm"
        >
            Enviar mensaje
        </button>
    </form>
</div>
